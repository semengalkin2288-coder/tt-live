services:
  - type: web
    name: tt-live
    runtime: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: python server.py
    healthCheckPath: /health
    envVars:
      - key: PYTHON_VERSION
        value: "3.11"